# Kept for reference only — docker-compose.yml uses the official `mongo:7` image,
# which already includes `mongosh`. Build this only if a custom image is needed.
FROM mongo:7

EXPOSE 27017

CMD ["mongod"]
